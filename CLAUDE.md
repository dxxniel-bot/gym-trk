# gym//TRK — project context (read me first)

> Single-file PWA gym + nutrition tracker. **Live:** https://dxxniel-bot.github.io/gym-trk/
> **Repo:** https://github.com/dxxniel-bot/gym-trk (branch `main`) · **Local (XboxL):** `X:\freelance\gym-trk\`
> Owner: dxxniel. Status: **deployed & functional, in active development.**

This file is the continuity anchor — a new chat opened in this folder should read it first.
Deeper project state / design history also lives in Obsidian: `G:\My Drive\AI_SYSTEM\projects\gym-trk\STATE.md` (syncs across machines via Google Drive) and the Figma reference at `G:\My Drive\AI_SYSTEM\imports\topics\fitness-calories-app.md`.

## What it is
A mobile-first web app (works on iPhone via "Add to Home Screen") to log gym workouts + nutrition. Rebuilt from the user's old Expo Go app (`ironLOG`). Aesthetic: **terminal/CLI, monochrome, JetBrains Mono**.

## How to run / deploy
- **Run locally:** static files; serve the folder and open the port. On **mopo** the repo is cloned at `G:\My Drive\AI_SYSTEM\projects\gym-trk\repo\` and there's a `.claude/launch.json` (one dir up) that serves it via `python -m http.server 4599 -d repo` for the Claude Code preview. On XboxL it was `X:\freelance\gym-trk\`.
- **Deploy:** `git push` to `main` → GitHub Pages auto-rebuilds (~1-2 min). No backend. **The iPhone is the test field** — camera/PWA need HTTPS, so always commit+push to deploy; localhost isn't enough for on-device testing.
- **Bump `sw.js` cache** (`gymtrk-vN`) on every shippable change so devices pick up the new build. Currently at **v234**.
- **Cambios de UI:** correr `node tools/ds-audit.cjs` antes y después (node puro, sin npm) — ningún commit sube los contadores P0/P1 de `DESIGN_SYSTEM.md` §18; capturas a 393×852 de las pantallas tocadas.
- **Design mode (live spacing editor):** open the app with `?design=1` to get a floating panel of sliders for the key spacing/radius tokens (`DESIGN_KNOBS`). It's a pure overlay — `applyDesign()` injects a `<style id="dz_style">` of `!important` overrides (and a `:root{--radius}` override) built ONLY from sliders you've touched, persisted in `localStorage.gymtrk_design`; it changes no source CSS. Tweaks apply on every load (even without the panel) until **reset**; `localStorage.gymtrk_designmode='1'` keeps the panel open across reloads (✕ clears it). "copiar CSS" exports the override block to bake the chosen values into the real rules.
- **Git identity** (set locally in the clone): `dxxniel-bot <dxxniel-bot@users.noreply.github.com>`. Commit directly to `main` (that IS the deploy flow — don't branch).
- **App nativa (iOS + Android, desde v222):** carpeta `native/` (Capacitor 8 + `@capgo/capacitor-health` 8.11.3, versiones fijas + `package-lock.json`). `ios/`, `android/`, `www/` y `node_modules/` NO se versionan (`.gitignore`): los workflows `.github/workflows/native-ios.yml` (macos-26, IPA sin firma + firma ad-hoc con el entitlement de HealthKit para Sideloadly/Impactor con Apple ID gratis) y `native-android.yml` (APK firmado con keystore permanente en secrets) los generan con `npx cap add` + `scripts/patch-*.sh`; solo corren con *Run workflow* (`target` probe|app) o tag `app-v*` — los minutos de macOS cuestan. **Nunca `npm install` en I:** (`scripts/guard-drive.cjs` lo bloquea): npm solo en el clon de X: o en CI. `capacitor.config.json` tiene el esquema/host CONGELADOS (cambiarlos = otro origen = datos locales "perdidos"). Pasos del dueño y puntos de decisión en `native/README.md`.
- **Continue from another machine:** `git clone https://github.com/dxxniel-bot/gym-trk` (or `git pull` in the existing clone), edit, push.

## Architecture
- **`index.html`** — the ENTIRE app (HTML + CSS + JS inline). Single-page, JS-rendered screens via a `render()` router on `state.screen`. ~all logic here.
- **`manifest.json` + `sw.js`** — PWA (installable + offline). `sw.js` is **network-first** for same-origin (online = latest deploy, offline = cache) — cache name bump (`gymtrk-vN`) forces refresh.
- **`*-prototype.html`** — static design references (superseded by `index.html`; keep for design history).

### Data model (localStorage key `gymtrk_db_v1`)
```
{ version, profile:{username,sex,age,weightKg,heightCm,activity,goal, since:'YYYY-MM-DD'(frozen first-use date, set in migrate)},
  settings:{unit, restSec(default 90), restBeep(default true), goals:{kcal,protein,carbs,fat,sodium,potassium,sugar,fiber,water,caffeine,alcohol,sleep(h, default 8)}},
  activeGym, gyms[], rotIdx,
  sleep:{ 'YYYY-MM-DD': hours },          // date = the morning you woke up; logged via openSleepLog(), counted in dbMass()
  steps:{ 'YYYY-MM-DD': n }, bodyweight:{ 'YYYY-MM-DD': kg }, stack:[...], goalHist:{...}, mealTags:[...], lastUsed:{...},
  machines:{ '<gymName>':{ '<key>':{brand,setup,na} } },   // key = machKey(ex): '#'+exId when the exercise has one (per split-DAY instance — bench press on day A and day B are scoped separately), else legacy lowercased name (ad-hoc exercises, or data captured before v209)
  split:{ name, days:[ {id,name,tag,metric,allowFailure, exercises:[{id(stable per-instance id, additive via migrate),name,muscle,muscles[]?,type('free'|'machine'|'smith'|'cable'|'bodyweight'|'cardio'; legacy 'pulley' read as an alias of 'cable', never rewritten on disk),machine,unilateral,startSide,assisted?,weighted?(bodyweight-only flags: assisted machine / weighted vest-dip, shown as chips, feed variantKey below),cardioSub?('treadmill'|'bike'|'elliptical'|'stair'|'rower'|'outdoor'|'swim'|'jump'|'spin' — only when type==='cardio'; part of variantKey),unit,note,sets(=defaultcount)}]} ] },
  sessions:[ {id,date,dayId,dayName,gym,type,loggedAfter,autoEnded?(true when you chose "cerrar" on promptIdleSession's modal for a session idle ≥10min — see below; NEVER set silently since v212),durSec,startTime,endTime,exercises:[{exId(links back to the split exercise instance that was live when logged; null for ad-hoc/replaced exercises — NEVER backfilled onto old sessions, resolved at read time only),name,muscle,muscles[]?(frozen fallback for ad-hoc exercises only — for split exercises, current //VOLUME reads muscles live from the split via exId, see musclesOf below),type,unilateral,startSide,assisted?,weighted?,cardioSub?,unit,note,machine,setup,sets:[{w,r,rir,unit,side,fsOn,extraW,isDrop,done,doneAt(epoch ms, per-set — drives idle detection, see sessionIdleEnd below),bw?(true on bodyweight sets; w becomes a legacy display snapshot, effW() always recomputes from the live bodyweight registry),/* cardio sets (type==='cardio') use: minutes,distance,speed,incline,resistance,rpm,level,floors,strokeRate,calories,hr,intensity — fields shown depend on cardioSub via CARDIO_SUBS */, _pf?(prefill suggestion, cleared on edit/done, never saved/counted)}]}]} ],
  meals:{ 'YYYY-MM-DD':[ {id,time,tag,name,qty, kcal,protein,carbs,fat,sugar,sodium,potassium,fiber,caffeine,water} ] },
  foods:[ {id,barcode,name,brand,serving, per:{...same nutrients per 1 serving}} ] }
```
- **No-data-loss rule:** keep the LS key stable, never clear it, migrate additively in `migrate()`. Editing code never touches LS. `[settings]` has export/import JSON (backup) + "log in" restores from a pasted backup.
- **Identity/variant engine (v206–v210, 2026-07-25; ladder extended v215):** exercise edits are per split-DAY instance, never global by name — `saveExEdit` merges into (never replaces) the exercise at `db.split.days[di].exercises[ei]`; editing "chest press" on day A never touches a same-named exercise on day B, and the mid-session editor no longer mirrors into the split at all (opt-in `ee_tosplit` checkbox, off by default). `variantKey(e)` = `canonType(type)+uni+assisted+weighted[+cardioSub]` — same name, different variant (machine vs. free, bilateral vs. unilateral / **UNI≠BI always chipped**, assisted vs. weighted, cardio subtype) is a fully independent history/prefill/%. Prefill (formerly "v68", now baseline-driven) and progress % both resolve against the **equivalent antecedent** via `baselineSetsFor()` — ladder (exact `exId` → same day+name+variant+occ → unique day+name+variant → same day+name+variant any occ → unique name+variant nearby → unique name+variant any position → name+variant most recent) — NEVER bare name alone (UNI/BI and free/machine stay isolated). `alignSetIdx()` then pairs today's sets to the baseline's like-for-like (drop/non-drop, side, ordinal) instead of a positional clamp, so an unmatched extra set shows no badge instead of a false one. `%`/progress badges use `sessionFatigueContext()`+`correctedE1RM()` (fresh-equivalent, discounting intra-session fatigue from primary AND weighted secondary muscles) with confidence tiers (`progConfidence`) so a weak match shows `~` and a wide mismatch shows direction only, never a fabricated number. Muscle-volume attribution (`musclesOf()`) resolves **at read time** from the split via `exId`, never mutating saved sessions — "lo que hiciste es inmutable; cómo se clasifica se resuelve en vivo": add triceps as a secondary to an existing exercise and every past session under that `exId` counts toward triceps in //VOLUME retroactively, with zero rewritten data. Drop sets are subseries of their parent working set (array stays flat, `isDrop` flag as before — the 4 existing 0.5-weightings are untouched), inserted via `dropInsertIdx()` immediately after their parent + any drops it already has (never appended to the end), numbered `1, 1.5, 2, 3` via `setLabels()` (chains to `.6`/`.7`); `emptySet()`'s clone-template no longer leaks `isDrop` onto a plain `[+ set]` add. Bodyweight exercises show `[BW <live kg>] [+lastre]`: `latestBodyweight()`/`effW()` always recompute from the most recent `db.bodyweight` entry (never a frozen per-set snapshot, including for past sessions), `bwMissing()` surfaces "BW sin peso" instead of silently scoring against 0. History editing (`state.screen='histedit'`, `state._histSessId`) reuses the exact live-session row components (`renderSetRow`'s new `actPrefix` param dispatches to isolated `h_*` handlers — `h_setf`/`h_done`/`h_fs`/`h_addset`/`h_dropset`/`h_delset` — that only ever touch the targeted past `db.sessions[]` entry, never `db.activeWork`) in a denser `.hist-compact` layout; it exposes full per-set correction but deliberately NOT structural edits (rename/machine-change/add-remove-exercise — those stay split/live-session decisions) and shows only an exercise-level badge, not per-set ▲▼. Permanent self-checks (`_identitySelfCheck`/`_dropsetSelfCheck`/`_baselineSelfCheck`/`_variantSelfCheck`/`_historyEditSelfCheck`) run on `?selftest=1`.
- **⚠️ CORRECCIÓN CRÍTICA — la colisión de `uid()` (v211–v214, 2026-08-04).** El motor de arriba llevaba 9 días reales en producción cuando el dueño reportó una cascada de síntomas (`+382%` en el %, mismo peso/series en TODOS los ejercicios de un día, //VOLUME contando 7 de 8 ejercicios de una sesión como tríceps, "me movió mis splits") que resultaron ser **un solo defecto, no ocho**, confirmado corriendo el motor real contra el respaldo real del dueño (`contexto/respaldo.md`, ver el plan archivado abajo): `uid()` (línea ~589) derivaba el id de la parte fraccionaria de `performance.now()`, que iOS/Safari clampea a 1ms de resolución (mitigación Spectre) — `%1` daba siempre `0`. El backfill de ids que `migrate()` corrió en v206 lo hizo en un bucle síncrono y le dio **el mismo id a los 39 ejercicios** del split real del dueño; `baselineSetsFor`/`musclesOf` resolvían con `confianza=exact` al primer ejercicio del día para TODOS los demás. Reparado en 4 bloques:
  - **v211:** `uid()` pasa a contador (`_uidN`) + `Date.now()` + random — nunca más deriva de una fracción de tiempo clampeada. `fixDuplicateExIds(d)`, dentro de `migrate()` y gateado por `d._idfix` (corre una sola vez): detecta ids duplicados en el split y re-acuña **todos** los colisionados (ninguno "es más dueño" que otro), luego re-vincula el `exId` de cada sesión afectada por (`dayId`+nombre) **solo** cuando el match es único — ambiguo o ausente → `null`, que el escalón por nombre+variante de `baselineSetsFor` sigue resolviendo sin adivinar. Respaldo previo a `LS_BACKUP`; estrictamente aditivo. `machKey()` ahora lee `ex.exId||ex.id` (antes solo `exId`, así que el editor del split —cuyos ejercicios llevan `.id`, no `.exId`— siempre caía a la clave por nombre sin importar la identidad real). Self-check nuevo: `_idSelfCheck`.
  - **v212 — ciclo de vida de sesión ("nunca finalizar sola, jamás perder una sesión"):** el auto-finalizado silencioso cerraba sesiones vivas en background (`checkAutoFinalize` corría en el heartbeat de 500ms) y el propio "continuar" quedaba en un bucle imposible de romper (el `doneAt` restaurado seguía viejo → el siguiente chequeo la volvía a dar por idle al instante). `autoFinalizeIfIdle` se separó en `isSessionIdle(w)` (solo detecta) + `finalizeIdleSession()` (el cierre explícito, mismo cálculo `endTime = última actividad + 10min`, nunca wall-clock). Nueva `promptIdleSession()` — modal **no descartable** (`openModal(html,'nodismiss')`, nuevo flag) con `[▶ continuar]`/`[■ cerrar a las HH:MM]` — disparada en `visibilitychange` + boot, **nunca en el heartbeat**. `continuar` marca `w.resumedAt=Date.now()`, que `sessionIdleEnd()` ahora considera (sin esto el bucle era determinista). Se quita la ventana de 30min de `resumesession`: cualquier sesión del historial se puede reabrir siempre. (Nota v210, sigue vigente: el bloque de sesión en la agenda es tappable → `vieweditsession`.)
  - **v213 — la sesión sale como se entrenó la última vez, no como dice el split:** `daySource(day)` (nuevo, compartido con el preview `//NEXT` vía `dayPreviewHTML` para que nunca prometa algo distinto de lo que `newWorkSession()` produciría) siembra la lista de ejercicios (nombres, orden, `exId`) desde `lastSession(dayId)` cuando existe una sesión real previa de ese día; el split solo manda la primera vez que se estrena un día. Por ejercicio, `splitExFor(prior,ei)` re-resuelve en vivo tipo/unilateral/assisted/weighted/músculo/máquina desde el split vía `exId` cuando el ejercicio sigue ahí (un cambio en el split se recoge la próxima vez, aunque la lista venga de la sesión anterior). Los ejercicios ad-hoc (`addExFromKnown`/`replaceExFromKnown`/`openAddExForm`, ambos modos) ya no reciben `exId:null` — acuñan su propio id estable para construir su propio historial/baseline/máquina sesión a sesión, aunque nunca entren al split (y sin heredar el historial del ejercicio que reemplazaron). `emptySet()` ahora también copia el RIR del set de origen (antes se omitía a propósito). Filas `_pf` en gris sutil (clase `.pf`, `opacity:.45`) hasta confirmarse: `setField` ya limpiaba `_pf` al tocar un input, pero elegir la MISMA opción en un `<select>` (rir/unit) no dispara `change` — nuevo listener de `click` en el dispatcher que confirma con solo interactuar el selector.
  - **v214:** vuelve `[↓ drop set]` con texto en la barra inferior junto a `[+ set]` (v205 lo tenía así; v210 lo dejó solo como un `↓` de 18px/`--o35` casi invisible — ahora 24px/`--o60`, sigue colgando de la ÚLTIMA serie de trabajo vía `dropInsertIdx`). `exerciseDirectory()` agrupa ahora solo por músculo **primario** (antes por `exMuscles()` completo, así que un ejercicio con secundarios aparecía duplicado bajo cada uno en el catálogo y en "elige ejercicio → músculo"); los secundarios siguen contando para fatiga/%VOLUME vía `musclesOf`/`sessionFatigueContext`, que nunca dependieron de esta función.
  - Las 4 correcciones verificadas contra `contexto/respaldo.md` (la DB real del dueño), no solo fixtures sintéticas — plan completo con diagnóstico línea-a-línea archivado en `I:\My Drive\AI_SYSTEM\claude-config\plans\eres-el-chat-del-composed-coral.md` (mismo archivo reutilizado del batch v206-v210).
  - **Known leftover dead code** (superseded, harmless, not yet swept): `lastLoggedSets`/`prevSetsForHist`/`exerciseProgress` (pre-baseline-ladder helpers, no longer called — real logic is `baselineSetsFor`/`histProgress`/`setProgress`), the `.srowm` CSS rule and `ss_f` input handler (orphaned by the history-editor rewrite), the `_w<1`/`contrib` secondary-weight badge in `openExerciseDirectory` (v214 made `_w` always `1`, so that branch never fires anymore).

### Screens (state.screen)
`landing` → `onboard` → `home` (gym: rotation w/ ‹›day picker, //NEXT, start/rest/skip, log-later, **//VOLUME** weekly-sets-per-muscle vs MEV/MAV/MRV, //STATS) · `workout` (live logging + bg duration; log-later mode = backdated + date/dur + advance-rotation toggle) · `macros` (kcal ring + macro rings w/ g⇄% & logged⇄remaining toggles + intake bars + bloat + food log w/ per-item P/C/F + per-meal totals + tap-to-detail) · `splitedit` (rename/add/remove days & exercises, edit exercise) · `history` (past sessions → view/edit/delete) · `settings` (//PROFILE editable, //TRAINING, //DATA export/import, rest-timer secs + beep toggle) · `progress` (REAL: weekly-consistency dots + tiles peso/volumen/e1RM/pasos/agua/sueño/kcal with lineCharts + Apple-Health-style drill-downs via `metricSeries(key,days)`/`openMetricDetail`) · `stack` (supps/skincare/meds with schedules).
**Rest timer (v139):** auto-starts when a set is marked done (`done` handler; skipped for `loggedAfter` sessions). State = `db.activeWork.restEnd` (epoch ms, persisted by `save()` — survives re-renders/reloads/iOS kills). UI = `#resttimer` capsule in static HTML BETWEEN `#view` and `#wfooter` (outside #view so `render()` never destroys it); a single global 500ms interval writes `textContent` only (`updateRestBar()`, never re-renders); `visibilitychange` recomputes from `Date.now()` (iOS freezes timers); expiry >30s late (app was killed) skips the notify. Audio = Web Audio chirp, `AudioContext` lazily created on the `done` tap (iOS gesture requirement) + `navigator.vibrate` for Android. Config: `settings.restSec/restBeep`.
**Overlays (v141/v142):** `showOverlay(html,{ms,onDone})` full-frame `.bootov`; `bootScreen()` = 1.3s terminal status on open (uptime/last session/weekly vol Δ/bw/sleep; once per app-open via `sessionStorage.gymtrk_boot`; only with username + ≥1 session; tap skips) → chains `snapRecap()` = once-daily day summary after 21h (kcal vs goal/pasos/sesión/agua/sueño; `localStorage.gymtrk_recap`=date; `?recap=1` forces for QA). Home shows `uptime` (days since `profile.since`) instead of streak — streak stays in progress. No FOMO/confetti by design.
**Screen-change fade (v145):** `go()` sets `state._trans` only when the screen actually changes; `render()` re-triggers `.fadein` (140ms) — NEVER on same-screen re-renders (every keystroke re-renders).
Gym stats helpers: `weeklySets(days)`, `VOL_LANDMARKS` (RP MEV/MAV/MRV), `volStatus()`, `isHardSet()` (RIR≤4), `e1rmTrend()` (Epley; surfaced in `progress`). `topLift()` is dead code (removed from UI, fn remains). Top-lift & raw-volume removed on purpose.
**Workout prefill (v68, resourced onto the baseline ladder in v208, RIR added in v213):** new sessions pre-fill each set's weight/reps/**rir**/unit/drop-flags/side, flagged **`_pf`** (a suggestion). RIR was withheld on purpose through v212; v213 copies it too (intensity-reference, same confirm-before-it-counts rule) per owner feedback. The SOURCE changed in v208 — `seedSets()` now pulls from `baselineSetsFor()` (the equivalent-occurrence ladder, see "Identity/variant engine" above) instead of the old flat `lastLoggedSets(name,uni)` (kept only as dead code, superseded), so the prefill is the correct antecedent occurrence, not just whichever session happened to log that name last; v213 additionally changed WHICH exercises get prefilled at all — `newWorkSession()` seeds the exercise list itself from `daySource()` (most recent real session of that day), not the split, so an improvised swap or a skipped exercise carries forward exactly as executed (see the v211-v214 correction above). `_pf` sets render at reduced opacity (`.pf`, since v213 — before there was no visual cue at all) until confirmed: `_pf` clears on edit (`setField`) or mark-done, or on tapping a `<select>` (rir/unit) even without changing its value (a `click` listener, since re-selecting the same option never fires `change`). `saveSession`'s filter (`(s.w||s.r)&&!s._pf`) and its `has` check drop still-`_pf` sets, so suggestions never save or count in stats until you confirm them. `[+ set]` duplicates the previous set (also `_pf`, and never inherits `isDrop` from it — see drop-subseries above). `emptySet(ex,side,isDrop,src)` — pass `src` to prefill.
Native-feel: viewport `maximum-scale=1,user-scalable=no`, body `position:fixed`, inputs `font-size:16px`, frame fills screen `@media(max-width:440px)`.

### Built-in base foods (`BASE_FOODS`)
A curated, code-embedded table (~259 entries) of generic **whole/basic foods** OpenFoodFacts lacks (huevo, res, cebolla, tortilla, frijol, aguacate, pechuga, arroz, café…). Mexican-staple + protein-leaning, broad coverage (dairy, poultry, beef/pork, fish/seafood, legumes, grains, veg, fruit, nuts/seeds, fats, sugars/condiments, snacks, supplements, beverages, prepared dishes). Each is already in canonical shape (`per100` for all MACROKEYS, `base:'g'|'ml'`, `sizes` with natural piece/serving). Built via a terse `add(name,kcal,p,c,f,opt)` helper (`opt`: `s`ugar `fi`ber `na`+`k` mg `caf` `sv`/`pc`/`ct` grams `sl` label `base` `en`=English alias). Values are ~per-100 g edible-portion USDA approximations (composite dishes are rougher). **Bilingual search** (user reported better MFP hits in English): every entry has an `en` alias and `bfNorm()` (accent-insensitive, lowercased) matches **name OR alt** — so "chicken"→Pechuga de pollo, "onion"→Cebolla. **Surfaced in search, not copied to LS:** matched in `openFoodAdd`'s inline `#fa_q` list (under a BÁSICOS group) and in `openNameSearch` (BÁSICOS above OFF PRODUCTOS). Pick → `data-act="pickbase"` → `openLog(food,{isNew:true})`; "guardar + loguear" copies it into `db.foods` (keeps its `bf_*` id, then shows under RECIENTES and is filtered out of BÁSICOS to avoid dupes). To extend: add more `add(...)` rows with an `en:` alias.

The big `BASE_FOODS` IIFE is hard to edit by exact-match; to regenerate it, write the new block to a temp file and splice with Node: `h.replace(/const BASE_FOODS=\(function\(\)\{[\s\S]*?\n\}\)\(\);/, newBlock)` (verify exactly one match first).

**`openFoodAdd(preset, lockTag)`** — the food-add sheet. Layout (top→bottom): optional COMIDA tag field + chips (a `#fa_tagtxt` input; **hidden** when `lockTag`) → `.fa-top` (search `#fa_q` + scan/search/register buttons, icon-over-label cards, equal width) → `.fa-listwrap` (RECIENTES/RESULTADOS list, fills + scrolls). `curTag()` reads `#fa_tagtxt` (works hidden). Opened from the **+ menu** (`addmeal`, editable tag) or from a **meal summary** (`addtomeal` → `openFoodAdd(tag, true)` → titled `add food · <tag>`, no tag editor, straight to the list).

### External (client-side, no keys/backend)
- **OpenFoodFacts** — barcode lookup (`/api/v2/product/{code}.json`) + name search (`/cgi/search.pl`). Barcode-not-found → offers **photo-of-label OCR** (primary) and name search (both link the food to the scanned code in `foods[]`).
- **Live barcode scanner** (`openBarcode()` → `startScan()`) — MFP-style: tap scan → camera opens immediately with center reticle overlay → auto-detects → found→`openVerify` (forward), not-found→`openRegisterMenu(code)` (register, barcode linked). **Architecture = live preview + background still-frame decode** (the key fix — Quagga LiveStream and `BarcodeDetector` both fail on iOS Safari): we own the `<video>` via `getUserMedia` (`facingMode:environment` 1280×720), then a loop every ~360 ms grabs the current frame to an offscreen canvas (center band, ~62% height, downscaled ≤1000 px) and decodes it **as a still image** — `BarcodeDetector.detect(canvas)` if available (Android/desktop, faster ~170 ms), else `Quagga.decodeSingle({src:canvas.toDataURL()})` (reliable on iOS). A `decoding` mutex serializes decodes so the loop and the manual button never run Quagga concurrently. **On hit:** `navigator.vibrate(55)` + green lock-on animation (`.scan-reticle.hit` → green frame `bcpop` + `✓` `bcchk`, scan line hidden) so the user can lower the phone, ~430 ms pause, then `doLookup`. **📷 capturar** button = thorough full-res decode of the *current preview frame* via `decodeImgSrc()` (multi-patch `decodeSingle`), or a file photo (`decodeBarcodeImage`) if no live camera — no longer ignored, since there's no running LiveStream to collide with. Manual numeric entry is the last fallback. `window._scanner={stream,video,timer}`; `stopScan()`/`closeModal()` clear the timer + stop tracks so the camera light goes off on close. **Replaced html5-qrcode (v5 ZXing → v6 Quagga2 LiveStream → v47 still-frame decode)** because live detection never worked reliably on iPhone.
- **Tesseract.js** (CDN, lazy) — photo→OCR of nutrition labels (FALLBACK when AI proxy off). `prepImage()` preps image; `parseNutritionText()` handles kcal-vs-kJ, comma decimals, g↔mg.
- **AI label reading (Gemini via Cloudflare Worker)** — `proxy/gymtrk-ai-worker.js` holds the OWNER's one Gemini key (free tier) so users need no key. App: `AI_PROXY_URL` const (empty ⇒ falls back to OCR), `aiPhoto()` POSTs a compressed JPEG dataURL → proxy → structured nutrition → verify; `labelPhoto()` routes to AI if configured else OCR; `fileToDataURL/sanitizePer/labelFallback`. To enable: deploy the Worker (see `proxy/README.md`), set `AI_PROXY_URL`. Verified via mocked proxy in preview. **NOTE: the AI schema returns the OLD per-serving shape and will be upgraded together with the canonical food-model refactor (below).**

## Done
Landing (create/login) · onboarding + auto-calc goals · gym home (rotation, start/rest/skip, prev, stats) · workout logging (muscle-grouped sets table: weight/unit/reps/rir, unilateral R/L pairs, drop set ↓, machine FS, +set/+exercise, mark-done, save→persist→advance rotation, abort) · **per-gym machine/brand/setup**: in a workout, the machine chip in each exercise header is tappable → modal to edit MARCA + SETUP or mark N/A; stored in `db.machines[gym][exercise]`, swaps with the active gym, "+ machine" placeholder to add on the fly (`openMachineEdit`, `machineRec`, `setMachine`, `exKey`) · macros (rings P/C/F, intake bars Na/K/sugar/fiber/water, bloat risk, food log) · food DB (barcode OFF + **live Quagga2 scanner**, name search + link-to-code, **photo OCR fallback when barcode not found**, text parse, manual, verify-before-add, qty, quick re-add) · PWA offline · export/import.

## Pending / next
- **✅ DONE (v25/v26) — canonical per-100 food model (math correctness).** Implemented + verified (porción=130, envase=1040, ×2=2080; legacy migration reproduces printed values; OFF/AI map to per100). **v26: split REGISTER vs LOG (user-required).** `openVerify`=REGISTER screen (name + porción size + macros-per-serving + envase/piezas; NO unit/qty) → "continuar → loguear" → `openLog`=LOG screen (unit `<select>` + qty + live totals). Saved foods (`pickfood`)→`openLog` directly; `editmeal`→`openLog` (meal items store `_food`+`_unit` for exact re-edit). `commitLog` replaces commitFood; `readRegister` builds canonical food (per-serving→per100). Below = the shipped design for reference.
  - **Store:** `food = { base:'g'|'ml', per100:{kcal,protein,carbs,fat,sugar,fiber,sodium(mg),potassium(mg),caffeine(mg)}, sizes:{serving?,piece?,container?}(grams/ml), servingLabel?, name, brand, barcode }`. (No `water` in per100 — for liquids, volume L = grams/1000 at log time.)
  - **Log = MyFitnessPal style:** unit dropdown built from `sizes` → `porción (30 g)` / `pieza` / `envase (240 g)` / `100 g` / `g`(or ml) + a qty field; grams = qty × unitGrams; nutrients = per100 × grams/100.
  - **UX RULE (user-required): the unit/serving is ALWAYS a `<select>` of default options — never free-typed.** The only typed fields are numbers (qty + the gram/ml size values), all `type="number" inputmode="decimal"` so the keyboard is numeric. No character-typing for units → no parse errors. Verified helpers: `gramsForUnit(food,unitKey)`, `foodUnits(food)`→[{key,label,grams}], `computeNutrients(food,unitKey,qty)`, `normalizeExtract(d)`→{base,per100,sizes,servingLabel} (converts whatever basis the label printed into per100 using serving_g / container_g; sets warnGrams when grams unknown).
  - **Touches (interlocking — do as one coherent, preview-verified change):** `mapProduct` (OFF→per100), `openVerify`+`readVerify` (base toggle + per100 inputs + size fields + live "por porción / envase total" readouts), `commitFood`, the pick-food→log path (add the unit dropdown), `aiPhoto`+Worker schema (extract basis, serving_g, serving_label, piece_g, pieces_per_container, container_g, servings_per_container, values; app normalizes), OCR/text/manual producers. **Migrate** legacy `foods[]` (`per`+`serving` string) via a `per100Of(food)` compat accessor; existing meal items already store absolute values so macros/totals are unaffected.
  - Was deliberately NOT rushed in the AI-proxy turn to avoid breaking the laptop's recent serving/liquid rework.
- **✅ DONE (v139–v145, 2026-06-09):** rest timer entre sets · sleep tracking (4th habit: log/tile/drill-down/goal) · terminal boot screen + uptime + snap recap (gamification tier 1, "disciplina visualizada") · design consolidation (canonical button roles 48/44px ≥44px tap targets, single `.field input` def, workout inputs 36px, tokenized `.ex`/`.item` spacing, terminal `// empty` states with `.ehint`, micro-labels ≥10px, 140ms screen fade). Verify on iPhone: rest-timer chirp + background recompute, sleep round-trip, boot/recap, workout grid at 393px.
- **✅ DONE (v146, 2026-06-10, other session):** 7 fixes del triage de 9 requests — ✕ quitar ejercicio mid-sesión (`delex`), `e1rmTrend` honra RIR vía `repsToFailure`, bodyweight seedea BW, edición completa de ejercicio mid-sesión (`openExEdit` con `liveExi`), agregar ejercicio en 2 pasos (músculo → `exerciseDirectory`), inputs nutricionales sin "0" pegado, foto de tabla en `openVerify`. Ver commit `7175dbf`.
- **✅ DONE (v147+v148, 2026-06-11) — //VOLUME counting fix:** `weeklySets` cuenta par unilateral R+L = 1 set duro (max por lado) y drop set = 0.5 · `MUSCLE_ALIAS` EN/ES (lats/espalda→back, hombros→delts, etc.) da landmarks MEV/MAV/MRV a nombres no canónicos · músculos sin landmarks dibujan barra contra escala neutral de 20 sets (antes barra siempre 0%) y sin badge vacío · ventana "7d" exacta (antes 8 días) en `weeklySets`/`stat7`/`muscleProgress` · sets/Δ a 1 decimal. **REGLA (v148, feedback del dueño):** `canonM()` se usa SOLO para el lookup de landmarks — las filas/claves de `byM`/`freq`/badge conservan EXACTAMENTE el nombre de músculo que escribió el usuario (v147 mostraba "lats" como "back" → reversado; nunca renombrar ni mergear los grupos del usuario).
- **✅ DONE (v206–v210, 2026-07-25) — motor de entrenamiento: instancias, drop sets como subseries, baseline contextual.** Overhaul grande en 5 bloques, ver "Identity/variant engine" arriba para el detalle técnico; plan completo (diagnóstico línea-a-línea, decisiones, riesgos, casos de prueba) archivado en `I:\My Drive\AI_SYSTEM\claude-config\plans\eres-el-chat-del-composed-coral.md`. Resumen por bloque: **A** identidad por instancia (`ex.id` estable, `variantKey`, `musclesOf` resuelto en lectura, autofill sellado, split↔sesión separados) · **B** drop sets como subseries (`dropInsertIdx`/`setLabels`, ya no se pueden "bloquear" entre sí, numeración `1,1.5,2,3`) · **C** baseline equivalente (`exIndex`/`baselineSetsFor`/`alignSetIdx`, % fresh-equivalent, honestidad del badge) · **D** variantes visibles (`free|machine|smith|cable|bodyweight` + `assisted`/`weighted`, chips, sin "+ machine" en peso libre, bodyweight `[BW][+lastre]`, máquina por instancia) · **E** historial con la UI de sesión (compacta, mismos componentes) + auto-finalizado a los 10 min de inactividad (⚠️ este auto-finalizado SILENCIOSO se corrigió en v212, ver abajo — ya no cierra sesiones sin preguntar). Decisiones de alcance explícitas: **sin** taxonomía manual de tipos de serie (la comparación se resuelve por posición/isDrop, no por etiquetas) y **sin** UI de planificación de drops en el split (`ex.plan[]` quedó fuera — el patrón de drops se re-siembra solo desde la sesión equivalente anterior vía `seedSets`/`baselineSetsFor`; una serie con drop que nunca se ha hecho no se puede pre-planificar, solo se agrega en vivo la primera vez). Verificado con `node -e`/harnesses de extracción (sin navegador esta sesión) + 5 self-checks embebidos (`?selftest=1`); **9 días de uso real expusieron un defecto crítico de raíz, ver la entrada v211-v214 justo abajo.**
- **✅ DONE (v216, 2026-08-13) — false ▼ progress-arrow bug + front-loaded `[unit]`.** Reported via 4 screenshots (`contexto/fix shit nigga.md`): sets logged with equal/more weight+reps+RIR than baseline still showed a red ▼. Root cause: confidence tier `'dir'` (`progConfidence`, fires when curCtx/prevCtx session-position fatigue context differs too much to trust — `dHard>8 || (dOrder>2&&noRest)`) already suppressed the `%` for being untrustworthy, but the arrow's SIGN still came from the same fatigue-*corrected* comparison (`correctedE1RM=raw/(1-discount)`, discount up to 0.35 → up to 1.54× inflation of whichever side was logged later/more-fatigued in ITS OWN session) — comfortably enough to flip a real raw improvement into a false ▼. Verified against the owner's real export (`gymtrk-export-2026-08-13.md`): "standing lateral rises" 12ago(75×9) vs 07ago-baseline(60×10, that session's 4th exercise after ~14 prior hard sets) — raw e1RM 102.5 vs 80 (up) but corrected 121 vs 133 (down, reproduces the bug exactly); same for "pendulum". **Fix:** new `dirSafePr(pr,tier,rawPr)` — at tier `'dir'` only, the arrow (not the suppressed %) falls back to the RAW e1RM comparison; `'exact'`/`'coarse'` tiers unchanged. Wired into `setBadgeHTML` + `pairBadgeHTML`. 2 new assertions added to `_baselineSelfCheck` (raw improvement never shows ▼ despite a more-fatigued baseline; a genuine raw decline still does). Verified 3 ways: `node --check`, `?selftest=1` in-browser (all self-checks OK), and calling the live `setBadgeHTML`/`setProgress` in a loaded page with the owner's exact real numbers — confirmed old code gave `dir:'down'` on both reported cases, new code gives `▲`/`var(--good)`. Separately: exercise's default **unit** now front-loaded as `[lbs]`/`[kg]` (`exDisplayUnit`/`exUnitTagHead`) at the very start of both the "cambiar ejercicio" picker rows and the live-session exercise header, in the exact font-size/weight as the exercise name (`.exhead .n`, 16px/800) — skipped for cardio/bodyweight where the unit concept doesn't apply. Cache `gymtrk-v216`. Full plan: `I:\My Drive\AI_SYSTEM\claude-config\plans\swirling-juggling-wand.md`.
- **✅ DONE (v215, 2026-08-06) — baseline day-2 + UNI/BI visible + drop layout + cardio type.** (1) `baselineSetsFor` extended so a full day of prior history (incl. reordered lists) resolves every exercise, not just the first nearby match. (2) `variantChips` always shows `[UNI]`/`[BI]` for strength; Pulldown UNI ≠ BI in `variantKey`/historial. (3) Per-row `↓` (`.dropadd`) removed — only bottom bar `[↓ drop set]`; grid last-col tightened so drop rows share layout with normal rows. (4) New `type:'cardio'` + `cardioSub` catalog (`CARDIO_SUBS`/`CARDIO_FL`/`weeklyCardio`); set bag uses minutes/distance/… not w/r/rir; excluded from `weeklySets`/`e1rmTrend`/`muscleWindowScore`/`setScore` path; baseline by same subtype; self-check `_cardioSelfCheck`. Cache `gymtrk-v215`. No bare-name baseline fallback (keeps variant isolation).
- **✅ DONE (v211–v214, 2026-08-04) — corrección crítica: la colisión de `uid()` + ciclo de vida de sesión.** Ver "⚠️ CORRECCIÓN CRÍTICA" arriba (junto al párrafo del motor v206-v210) para el detalle técnico completo; mismo plan archivado que arriba, reescrito con el diagnóstico nuevo. Resumen: el motor de v206-v210 era correcto, pero `uid()` (roto por `performance.now()` clampeado en iOS/Safari) le dio el mismo id a los 39 ejercicios del split real del dueño, y el motor de baseline/músculos resolvía con total confianza al ejercicio equivocado — de ahí el `+382%`, "mismo peso en todos los ejercicios", //VOLUME contando todo como tríceps. **v211** repara los ids (`fixDuplicateExIds`, gateado, corre una vez) y arregla `uid()`/`machKey()` de raíz · **v212** reemplaza el auto-finalizado silencioso por `promptIdleSession()` (pregunta, nunca decide; nunca más de 30min de límite para reanudar) · **v213** siembra la sesión nueva desde la última sesión REAL de ese día (`daySource`), no desde el split — un ejercicio improvisado ya no se pierde la próxima vez; RIR se copia en el prefill; filas `.pf` en gris hasta confirmar · **v214** devuelve `[↓ drop set]` visible y `exerciseDirectory()` agrupa solo por músculo primario. Verificado en dos niveles por bloque: self-checks aislados (código real extraído y ejecutado, no reimplementado) + el motor completo corrido contra `contexto/respaldo.md` (la DB real del dueño) confirmando el antes/después exacto de cada síntoma reportado. Clon `I:\My Drive\...\gym-trk\repo` (que estaba 9 días atrasado, sin v206-v210) sincronizado con `origin/main` — su único cambio sin commitear (un experimento roto que llamaba a `musclesForSessionExercise`, función inexistente) quedó preservado en `git stash`, no descartado. Pendiente: validación visual en iPhone — nada de esto se probó en navegador real esta sesión.
- **✅ DONE (v217, 2026-08-21) — progreso de máquina gym-scoped + guardia de unidad + `[uni]`/`[bi]` al frente.** Reportado el mismo día (entrenó jueves 20 en Club Campestre en vez de AGON GYM, registró el pushdown en placas porque esa polea no marca kg, y aun así ▼ rojo). Dos defectos de raíz en el motor de progreso, más una regresión de v216: (1) `baselineSetsFor`/`exIndex`/`exIdentity` nunca conocían el gym de la sesión — el ▲/▼ por ejercicio buscaba "el anterior" por nombre+variante entre TODOS los gyms, cruzando máquinas distintas. **Fix:** `exIndex()` guarda `gym` por fila, `exIdentity()` lo propaga, y `baselineSetsFor`'s `inWindow` gana `gymOK(r)` — conservador: solo bloquea cuando AMBOS lados declaran gym y difieren (nunca rompe historial legacy sin gym); `seedSets()` hereda el mismo gym para el prefill. Un gym nuevo simplemente arranca "sin baseline" (sin flecha) hasta tener su propia historia — correcto, no un bug. (2) La comparación tampoco miraba `s.unit`: comparaba magnitudes crudas sin importar kg/lbs/pla (`10 pla` vs `35 kg` → 10<35 → rojo). **Fix:** nuevo `unitN(s)`; guardia en `setProgress`, `pairBadgeHTML` e `histProgress` (vía `exDisplayUnit`) — unidades declaradas y distintas ⇒ sin badge, nunca una comparación falsa. **Alcance deliberado:** el gym-scoping y la guardia de unidad tocan SOLO el progreso por ejercicio (baseline/prefill); `weeklySets`/**//VOLUME**, `muscleWindowScore`/fatiga siguen sumando EN GENERAL entre todos los gyms (decisión explícita del dueño — "cada gym lleva su progreso en máquinas, pero los sets por músculo y la intensidad se cuentan en general"), verificado que un set en Club Campestre sigue contando para el tricep de la semana. (3) Regresión de v216: el corchete frontal (`class="n"`, fuente del nombre) mostraba la UNIDAD (`[kg]`/`[lbs]`) en vez de la lateralidad que el dueño pidió ahí. **Fix:** nuevo `exLatTagHead(ex)` → `[uni]`/`[bi]` al frente (mismo font/tamaño que el nombre) en el header de sesión (`renderExercise`), historial (`renderHistExercise`) y el picker (`openAddExStep2`, antes mostraba `[kg]`/`[lbs]` ahí); `variantChips` deja de duplicar `[UNI]`/`[BI]` como chip tenue. `exUnitTagHead`/`exDisplayUnit` quedan (el segundo aún usado por la guardia de unidad); la unidad se ve únicamente en el `<select>` de cada serie, como siempre. Verificado: `node --check`, `?selftest=1` (8/8 self-checks OK, incl. 3 asserts nuevos de gym/unidad en `_baselineSelfCheck` y 2 reescritos en `_variantSelfCheck` para el nuevo `exLatTagHead`), y reproducción real en preview (sesión Club Campestre en `pla` sin historial propio → "sin baseline", sin flecha; el mismo pushdown en AGON en `kg` sigue comparando contra AGON; `weeklySets` sigue sumando 2 sets de tricep entre ambos gyms). Cache `gymtrk-v217`.
- **✅ DONE (v218, 2026-09-17) — drops congruentes (sin full stack heredado) + historial de drops.** Reporte del dueño 2026-09-16: tras W1 (100 lbs) + drop 1.5 (60), `[+ set]` creaba la serie 2 en *full stack* y con 60. Raíz: `dropset`/`h_dropset` forzaban `fsOn:true` en cada drop, `addset` clonaba el ÚLTIMO elemento (el drop) y `emptySet` heredaba `fsOn` (solo podía encender); `seedSets` lo re-sembraba cada sesión; `setField` propagaba el peso tecleado a toda serie posterior sin distinguir drop/normal. **Fix:** `fsOn`/`extraW` son propiedad de la SERIE DE TRABAJO (`emptySet` solo los copia trabajo→trabajo; un drop nace sin FS; apagar FS limpia el `extraW` oculto que seguía contando en `effW`); `[+ set]` usa `workSource()` (última serie de trabajo del mismo lado); `[↓ drop set]` usa `insertDrop()` → `dropSource()`: (1) drop previo de la sesión en la misma posición de cadena (`.5`/`.6`) cuya madre pesó igual, (2) drop equivalente de la sesión baseline, (3) ratio drop/madre (sesión → baseline → mediana de `dropRatioHistory()`) × peso de la madre redondeado por unidad (`roundLoad`: kg 2.5, lbs 5, pla 1), (4) vacío — siempre `_pf`. `setField` solo propaga a series del mismo tipo y re-deriva el drop hijo pendiente si cambia la madre (110 → 65). Drops muestran chip `DS` fijo (`fsCellHTML`, sin toggle) y "60% de la serie" (`dropPctTxt`) en vivo e historial. `migrate()` → `fixLeakedFullStack` (gateado `_fsfix`, snapshot previo en `gymtrk_db_v1_pre218`): limpia `fsOn` de drops y de series de trabajo con FS sin extra justo después de un drop. `gymMatch()` extraído de `baselineSetsFor`. `_dropsetSelfCheck` usa los handlers reales + casos del reporte (100 tras drop, sin FS, cadena .5/.6, unilateral por lado, FS+extra trabajo→trabajo, reparación). `_cardioSelfCheck` ya no se pudre con el calendario (fecha relativa). Verificado: `node --check`, `?selftest=1` 8/8, flujo real en preview (100 → drop 60 → `[+ set]` = 100 sin FS → drop 60 → madre 110 → 65; sesión nueva desde historial con madre 120 → drop 70).
- **✅ DONE (v219, 2026-09-17) — motor de tensión: capacidad (▲▼%) + tensión efectiva T.** Decisión del dueño: la flecha/% mide **capacidad** ("hacer lo mismo con más esfuerzo = pérdida de rendimiento y por ende de tensión mecánica potencial"); la tensión efectiva APLICADA es otra lectura. **Capacidad:** Epley+RIR sin cambios (F ≡ RIR0), pero `histProgress`/`sessionProgAgg` ahora usan `capacityProgress()` — solo series de TRABAJO alineadas (`alignSetIdx`), con RIR conocido, misma unidad, ratio medio con contexto de fatiga (crudo si `progConfidence` = 'dir'); un drop o un set sin RIR ya no mueve el %. `isHardSet` acepta 'f'. **Tensión (`TENS`, perillas):** por rep `P(x)=exp(−x/τ)` con τ=3 sobre el RIR de cada rep (k+r−j); `setER` = Σ + F·0.5·P(0) (F = RIR0 + media rep); RIR vacío → k=2 marcado `~`; `setTension` = I·ER·gate(I), I = effW / `refE1RMFor()` (mejor e1RM de series de trabajo con RIR en 90 d de la misma identidad+gym+unidad, o de la sesión si es mayor), gate 0 <25 % → 1 ≥40 %. Últimas 5 reps = 81-100 % del crédito; al fallo (1RM 100): 30RM 1.76 · 15RM 2.34 · 10RM 2.55 · 8RM 2.59 · 6RM 2.54 · 4RM 2.29 · 4F 2.73. Unilateral = media de lados; drops con su carga. `setFatigueCost` (índice de carga para recuperación). Agregados: `exTensionSets`, `sessionTension` (caché por sesión en `bumpIdx`), `muscleWindowTension` (vía `musclesOf`, reemplaza a `setScore` en `muscleWindowScore`; `setScore` queda deprecado sin consumidores de UI), `tensWeek`. Fuentes en el comentario del código: Robinson 2024, Refalo 2023/2024, Lopez 2021, Schoenfeld 2017, Lasevicius 2018, Pareja-Blanco 2017. **UI:** fila `▼−4% · T2.5` (`tensTag`), chip `T 6.7` en el header (`exTensChip`, se refresca en `updateProgBadge`), historial y hoja de sesión con T por ejercicio y total, //VOLUME con `T` por músculo y ▲▼ = T 7d vs previos, tile nueva "tensión · sem" (config `tension`) y T por músculo en el detalle de volumen. **Tonelaje en kg** (`kgLoad`: lbs→kg, placas excluidas) en `progWeekly`, wrap y export; export con T por ejercicio/sesión y F impreso como F. Self-check nuevo `_tensionSelfCheck` (ER 3.40/2.44/3.10, orden F>0>1>3>5, 4F/30@0≈1.55, pico 6-12RM, gate, RIR vacío, caso del dueño 100×8@RIR2→@RIR0 = capacidad ▼ con T ↑, drop no mueve el %). Verificado: `node --check`, `?selftest=1` 9/9, preview con 2 sesiones reales (bíceps ▼−4% · T 6.7, remo unilateral ▲+10% · T 1.8, //VOLUME, tile, detalle, hoja, editor, export).
- **✅ DONE (v220, 2026-09-17) — capa de datos de salud (independiente del transporte).** El dueño usa un **Google Fitbit Air** en iPhone (Google Health escribe a Apple Health pasos, FC, FC reposo, sueño+fases y ejercicio — **no HRV**); una PWA no puede leer HealthKit, así que la lectura automática llegará con la app nativa (plan, Fase E). Esta versión deja lista la capa de datos: `db.health` (`healthShape`, aditivo en `migrate`, **no** cuenta en `dbMass`) con `sources`, `sync`, `daily`, `sleep` (por fecha de despertar, con fases), `workouts`, `sessHR`, `src` (procedencia por fecha) y `log` (50). Todo entra por **`ingestHealth(payload)`** (formato único para el sync nativo y para "importar JSON de salud"; `window.gymtrkIngestHealth` para el puente): pasos escriben `db.steps` salvo días tecleados (`src.steps='manual'`, que ahora marca `editsteps`; el Atajo marca `shortcut`); sueño escribe `db.sleep` solo si el día está vacío o ya era automático (bloques con `src`, `hours` = tiempo DORMIDO vía `asleepMin`) — lo manual jamás se pisa, y el modal ya no re-deriva las horas de un bloque sincronizado que no se editó. Sesiones guardan `startMs`/`endMs` (`saveSession`, `finalizeIdleSession`); `sessionWindow()` deriva la ventana de las viejas por fecha+HH:MM y `doneAt`. **FC:** `computeSessHR` guarda resumen (media/mín/máx, `medDt`, cobertura, serie reducida ≤240 puntos, final a las 48 h) y **por serie solo si la resolución lo permite** (`HR_SET_MAX_DT` = 15 s): ventana que termina en `doneAt` y empieza en max(`doneAt` previo, duración estimada) → `pre` (mediana 20 s antes), `peak` (hasta 30 s después, por la latencia), `post60`, `rec60`, `q`. Con ~1 muestra/min solo resumen. `matchWorkouts` liga el workout de Salud a la sesión (solape ≥50 % o inicio ±20 min), nunca crea sesiones; `pruneHealth` (serie reducida 180 d). **UI:** Settings → //SALUD (estado, último sync, fuentes, conteos, log, importar JSON, atajo), hoja de sesión con gráfica SVG de FC + marca por serie + "FC media · máx · min · kcal · fuente" y "FC pico · rec 60 s" por ejercicio, editor de historial con `FC 78→153 · −56` por serie (`hrTag`), tiles "FC reposo", "HRV", "energía activa" (solo con datos; config `rhr/hrv/kcalact`). Arreglos: `selfcheckInsights` leía `+db.sleep[d]` (NaN desde v164) → `sleepHours`; el detalle de métricas ya no elige un día sin registro como "menor día"; FC reposo muestra "día más alto/bajo". Self-check `_healthSelfCheck` (conteos, pasos manuales intactos, sueño por fecha de despertar con horas dormidas, FC 5 s → por serie plausible, workout ligado, idempotencia, sueño manual intacto, ventana legacy, FC 1/min → solo resumen). Verificado: `node --check`, `?selftest=1` 10/10, sesión real + payload simulado (gráfica, 5 series con FC, //SALUD, tiles, detalle).
- **✅ DONE (v221, 2026-09-17) — recuperación, causas del ▼ y correlaciones de rendimiento.** "Todo debe tener sentido y un propósito" (dueño). **`readinessFor(date)`** = "preparación estimada" 0-100, descriptiva (nunca entra a la matemática de capacidad/tensión): sueño vs meta (−25 por hora faltante, −10 si 1 h bajo la media de 14 d) · FC reposo en z vs 28 d (necesita ≥7 días) · HRV en z de ln (solo si alguna fuente la da — el Fitbit Air no) · carga aguda 72 h vs crónica 4 sem (`setFatigueCost` vía `loadOnDate`; solo con ≥4 días de entreno en 28) · comida del día previo (kcal vs meta ±15 %, proteína <80 % de meta) · ánimo si se registró. Pesos `READY_W`, los faltantes se redistribuyen, `conf` = fracción de peso presente, caché 60 s (`_readyCache`, limpiada en `bumpIdx`). UI: línea "recuperación 43 · sueño 6.2h (−1.8) · FC reposo +4 · …" bajo //NEXT (hoy) y en el contexto de la sesión en vivo; toque → `openReadiness` con el desglose. **`downContext`** ("esa disminución tiene una causa"): cuando la capacidad sale ▼ lista lo que fue distinto — sueño bajo, FC reposo alta, HRV baja, carga 72 h alta, comida baja ayer, mismo músculo entrenado <48 h antes, ≥4 series duras más antes que la vez pasada, ≥3 h de diferencia en la hora — en la hoja de sesión y en el editor de historial ("▼ posible: dormiste 5.1h · FC reposo +7"); `histProgress` guarda su baseline (`_base`, no enumerable). **`performanceInsights`**: capacidad media por sesión vs sueño previo, FC reposo, kcal/proteína del día previo, carga 72 h, hora de entreno y ticks de suplementos (Pearson, ≥8 sesiones, |r|≥0.3) → //RENDIMIENTO en Progreso ("correlación, no causa"). **Fix de riesgo real:** `_historyEditSelfCheck` reemplazaba `db.split`/`rotIdx` y llamaba a `finalizeIdleSession` (que guarda) sin restaurarlos — abrir `?selftest=1` con datos reales dejaba un split de prueba "push"; ahora restaura ambos. Self-check `_readinessSelfCheck`. Verificado: `?selftest=1` 11/11 (split intacto en memoria y en localStorage tras la recarga), escenario de 3 semanas en preview (línea, desglose, causas en hoja y editor), correlación sintética detectada (r=1, n=11).
- **✅ DONE (v234, 2026-09-18) — diagnóstico por músculo: estímulo vs recuperación (PT2, fase 4 de 4).** Lo que pidió en `fix.md` PT2: "si la recuperación va lenta, ver por qué; si va sobrado, dar sugerencias… que todo tenga sentido". Construido después de DS-3, con el sistema de diseño (componente nuevo §7.16).
  - **`muscleDiagInput(id)`** junta la evidencia de un músculo canónico: volumen 7d y 7d previos (series, RIR medio y cuántas series con RIR), últimas 3 exposiciones de 28 d (capacidad, horas desde la anterior, la ventana que aplicaba: observada › estimada con su dosis, y las **banderas del día**), capacidad de 14 d, capacidad tras <6 h de sueño vs el resto (todas sus exposiciones), el caso más reciente de **secuencia** (un primario que iba tras otro del mismo músculo a RIR ≤1, y si la vez anterior también) y el reparto de longitud de 28 d con cuánto viene de perfiles **sugeridos** sin confirmar (`lenSrc` nuevo en `muscleExposureIndex`).
  - **`diagnoseMuscle(inp)`**, pura, 9 reglas con umbrales en `DIAG` que solo se disparan con evidencia: **fatiga acumulada** (series ≥ +25 %, RIR ≥ 0.5 más bajo y capacidad ≤ −3 %, sin banderas del día) · **antes de recuperarse** (menos horas que su ventana y ≤ −3 %) · **la baja coincidió con tu día** (sueño, comida, FC, carga) · **sobre MRV** · **2.º ejercicio tras uno al fallo** (solo si la vez anterior no iba así) · **lejos del fallo** (RIR medio ≥ 3 con ≥ 4 series con RIR, capacidad sin caer y sin banderas; Refalo 2023, Robinson 2024) · **bajo MEV 2 semanas** (si además va lejos del fallo: "primero acércate al fallo") · **rinde menos tras dormir poco** (≥ 3 exposiciones por lado, ≥ 3 puntos) · **sin trabajo en estiramiento** (≥ 70 % de la longitud clasificada; `~` si se apoya en sugerencias). Sin nada que decir → "en orden" con lo verificado; con una sola exposición → "pocos datos". Nunca una puntuación; nunca cambia un número del motor.
  - **UI:** bloque **DIAGNÓSTICO** al inicio de `openMuscleDetail` (hallazgo por severidad, evidencia, `→` qué hacer, nota "solo aparece lo que tu historial respalda"); en **//MÚSCULOS**, el hallazgo principal en corto bajo la recuperación (`diagRowHTML`; MEV/MRV no se repiten porque ya están junto al nombre, "en orden" no ocupa renglón). Caché `_diagCache` (se limpia en `bumpIdx` y al cambiar de día): 14 músculos en ~18 ms.
  - **`downContext` por id canónico:** las banderas del día salen de `dayFlags(date)` (una sola definición, mismos umbrales) y "mismo músculo entrenado hace X h" compara ids canónicos primarios, no etiquetas; antes, con `delts`, una elevación lateral contaba como el mismo músculo que un press.
  - Self-check nuevo **`_diagSelfCheck`** (19 en total): cada regla con su caso de evidencia y su caso de ruido que NO la dispara, orden por severidad, `~` por sugerencias, integración con exposiciones reales (RIR 4 → lejos del fallo, RIR 1 → no) y `downContext` separando deltoides lateral de frontal. Verificado en preview con 43 sesiones: 14 músculos, hallazgos solo donde hay evidencia, detalle sin desborde, "▼ posible" del historial intacto.
- **✅ DONE (v233, 2026-09-18) — DS-3: componentes del sistema de diseño.** Auditor: radios fuera de escala 16 → 0, sombras fuera de token 10 → 0, selectores repetidos 18 → 0 (el auditor ahora cuenta solo reglas propias de nivel superior, no variantes de `@media` ni base compartida; los anillos `0 0 0 Npx` e `inset` son bordes, no sombras; acepta un archivo como argumento para comparar contra un respaldo). **Chips:** la cajita alrededor de "bajo MEV" era un segundo `.vst` → estados como texto sin caja (`.vst`, `.lpr` PR, `.pill` retención); seleccionables en píldora de 36 px (`.spc`, `.chip`, `.ag-chip`). **Inputs:** selects del perfil (`.pfsel`, `#pf_gym`, `.pfw` que iba sin estilo), horas de sueño y rango de fechas del detalle → familia formulario 44/r12/1px/16; el select del mapeo de músculos usa 13 px (a 16 cortaba "grupo deltoides"). **Footer de sesión** a 44 px con radio (antes ~30 px y 10 px de letra); `↩` pasa a `.undo` sin el borde rojo de abort; los −15/+15/skip del descanso se ven de 32 y se tocan de 44 (`::after`). **Glass:** `.glass`/`.glass-strong` en el marcado de nav, sheet (`openModal`) y toast; ya no se copia el material (ajustes por pieza `.sheet.glass-strong`, `.toast.glass-strong`). **Sheets animados:** `openModal` desde cero baja desde arriba (280 ms `--ease-out`) y `closeModal` quita el `id` al instante y deja un fantasma sin clics que sube en 180 ms; abrir encima de otro o justo tras cerrar (`closeModal(); openX()`) es cambio de contenido, sin animación. **Resto:** balance mantenimiento/volumen neutro (déficit sigue azul); trazos SVG en cuatro valores (dato 1.4/1.8, referencia 1, rejilla .5; íconos 1.6); 🗑 → `✕ borrar` (`.cancel.danger`), 🔒 → `[fijar]`/`[fijo]`, 📸 fuera; sin glow en la barra de intake ni en el escáner; FAB y fantasma de arrastre en `--z-float`; separador de listas .5px `--o10`; 18 selectores duplicados fusionados y CSS muerto fuera (`.rowend`, `.pfv`, `.pwk`, `.ptla`, `.prng`, vista previa de sesión, `.srowm`, `.shset`, `.mbanner`/`.mb-*`, `.prow`). Verificado: `?selftest=1` 18/18; medidas en DOM (footer 44, descanso 32+44, selects 44, chips 36, `.vst` sin borde, toast centrado con borde por tipo, sheet con borde solo inferior, entrada `sheetin`, cambio de contenido sin animación, fantasma que se borra); sin desborde en 7 pantallas y 9 sheets.
- **✅ DONE (v232, 2026-09-18) — DS-2: tipografía, tracking y ritmo del CSS al sistema.** Todo el bloque CSS pasa por tokens: `font-size` literales en CSS 229 → 0 (240 usos de `var(--t-*)`, antes 31; 22 valores fuera de escala remapeados — p. ej. `.pval` 23→22, `.mdval` 36→34, `.dchk` 15→16 — y 22 exenciones marcadas con `/*ds:exempt*/`: números de anillos, FAB, overlays boot/wrap/escáner, panel de diseño), 51 `letter-spacing` a 4 roles (`--ls-caps/title/num/ui`, nuevo `--ls-ui:.03em`), 118 espaciados impares al par más cercano (6 y 10 quedan como medios pasos; los márgenes negativos que centran puntos no se tocan), márgenes laterales de barras (footer, descanso, AHORA) y nav a `--sp-px`. `ds-audit.cjs` aprende `/*ds:exempt*/`. Verificado: `?selftest=1` 18/18, medidas del DOM (nombre de ejercicio 16/800, inputs 12/36, barras a 18 px), sin desborde horizontal en 7 pantallas y 7 sheets.
- **✅ DONE (DS-0 + v231, 2026-09-18) — sistema de diseño: `DESIGN_SYSTEM.md` + auditor + corrección P0.** El dueño pidió una guía estética detallada ("que todo parezca de un mismo ecosistema") que sirva para corregir lo actual y para cualquier implementación futura, más un reporte de 21st.dev. **DS-0:** `DESIGN_SYSTEM.md` (fuente única; reemplaza a `STYLEMAP.md`, que quedó como puntero): definición congelada, principios, jerarquía de identidad, tokens con valores cerrados, cada componente con función/anatomía/sí-no, gráficas, lenguaje de honestidad, movimiento, íconos, accesibilidad, mapa por pantalla, excepciones, prohibidos, protocolo para features nuevas, auditoría, tooling y política de 21st.dev (solo comportamiento reescrito en JS puro; todo allá es React+Tailwind con clave de API; solo `number-flow` justifica dependencia), más la línea base medida y el plan DS-1…DS-6. **`tools/ds-audit.cjs`** (node puro) mide tokens vs literales, valores fuera de escala, variables sin definir, pesos no cargados, colores literales, selectores repetidos y `style=""` por función — línea base 570 `style=""`, 342 `font-size` literales vs 31 tokens. **DS-1 (v231):** `.mdetail` estaba definido dos veces y los 7 detalles de métrica heredaban la sangría de 22 px + borde izquierdo del detalle de comida (la línea vertical rara) → el de comida es `.mdmeal`; `.mdetail-wrap .sheet` nunca coincidía → `.sheet.mdetail-wrap` (88vh); `--o15` (borde que nunca se pintó) quitado, `--o25`/`--o55` → escala; **peso 600 ×10 → 700** (no estaba cargado); **`toast(msg,type)`** con borde por tipo (✓ verde, ⚠/error rojo con 5 s y cierre al tocar, resto neutro; `role` status/alert) — antes siempre verde; verde/rojo decorativo fuera (AUTO, ↺ última vez, punto de ánimo, láser del escáner); `manifest.json` a #000; **capas por token** (`--z-*`): el scrim de los modales ahora tapa la nav; tokens nuevos `--t-hero`, `--ls-*`, `--s7/--s8`, `--r-mark`, `--shadow-float`, `--dur-*`, `--ease-*`. Auditor: P0 detectables 0. Verificado: `?selftest=1` 18/18, detalle de pasos a 393×852 sin la línea, detalle de comida intacto, tres tipos de toast, modal z40 > nav z30.
- **✅ DONE (v230, 2026-09-17) — recuperación por músculo (estimada → observada) y tablero //MÚSCULOS (PT2, fase 3 de 4).**
  - **`muscleExposureIndex()`** (caché `_muscExpCache`, limpia en `bumpIdx`): por cada sesión, lo que recibió cada músculo CANÓNICO — series duras con las reglas de //VOLUME (par unilateral = 1, drop = 0.5) × peso de `exTargets` (primario 1×, secundarios con su peso), directas, T, RIR medio, series a RIR ≤1, reparto de longitud y perfil (`exProfileOf`), y la capacidad de sus ejercicios PRIMARIOS vs su sesión equivalente (`histProgress`). Ventana real de la sesión con `sessionWindow`.
  - **Ventana estimada** (`recoveryEstimate`): base `MUSCLES[id].rec` × dosis — series (0.8–1.35), proximidad al fallo (+30 % si todo a RIR ≤1, Morán-Navarro 2017) y trabajo en estiramiento (+15 %). **Ventana observada** (`observedRecovery` sobre `musclePairs`): pares de exposiciones seguidas → capacidad de la 2.ª según horas de descanso, por tramos (<48 · 48–72 · 72–96 · 96–168 · >168 h); recuperado = primer tramo con ≥2 pares cuya mediana ya no cae (≥ −1 %); **mínimo 6 pares** o no hay observada. **Estado** (`muscleRecoveryState`): horas desde la última exposición vs la ventana (observada si existe) → fresco / casi / ~% recuperándose.
  - **//MÚSCULOS** en Progreso (antes de //RENDIMIENTO, `musclesSectionHTML`): una fila por músculo real (nombre = SU etiqueta si una sola mapea, si no el corto: su `delts` aparece como `front delts`/`side delts`), series duras 7d contra MEV/MRV, RIR medio y estado de recuperación en color; aviso si hay ejercicios con músculo sin resolver (lleva a "músculos"). **`openMuscleDetail(id)`**: VOLUMEN (series, directas/indirectas, sesiones, guía RP) · ESTÍMULO (T vs 7d previos, RIR medio y % a RIR ≤1, reparto de longitud y perfil con lo "sin definir") · FATIGA (series en 72 h vs su promedio de 4 semanas) · RECUPERACIÓN (última exposición, ventana estimada con su dosis, observada con nº de pares y capacidad por tramo, estado) · RENDIMIENTO (últimas 5 exposiciones con ▲▼). Sin puntuación única, a propósito.
  - **Gym (home):** bajo la línea de preparación de //NEXT, los músculos del día y su estado (`dayMusclesLineHTML`, toque → Progreso).
  - Choque de clases evitado: `.mrow` ya era de las barras de macros → el tablero usa `.mscrow/.msctop/.mscn/.mscv/.mscrec`.
  - Self-check nuevo **`_recoverySelfCheck`** (18 en total). Verificado en preview con 10 semanas / 43 sesiones con su vocabulario: 14 músculos, `hams` con ventana observada 72–96 h (13 pares) frente a la estimada 63–127 h, detalle completo y línea del gym.
- **✅ DONE (v229, 2026-09-17) — perfil del ejercicio: resistencia, longitud muscular y músculos del motor (PT2, fase 2 de 4).**
  - **Dos propiedades distintas** (PT2): **perfil de resistencia** (`asc` más duro al final/acortado · `desc` al inicio/estirado · `mid` a mitad · `unk` "no sé") y **longitud muscular** (`long` estirado · `mid` · `short` acortado · `mixed` · `unk`). Constantes `RP_OPTS`/`LEN_OPTS`.
  - **Dónde vive** (decisión del dueño): máquina/polea/smith → resistencia **por gym** en `db.exCat[name].rpGym[gym]` (la leva o polea de cada gym es otra); peso libre/corporal → una sola en `exCat.rp`; longitud en `exCat.len` (es del movimiento). `profileFor(ex,gym)` / `exProfileOf(sw,ei)` resuelven en lectura con el gym DE ESA sesión: lo tuyo › sugerencia › desconocido, con procedencia (`user`/`suggested`/`unknown`). Nunca se copia a las sesiones y queda fuera de `variantKey`.
  - **Sugerencias solo con evidencia sólida** (`EX_HINTS`, decisión del dueño "sugerir, tú confirmas"): RDL/SLDL con barra (⬇, estirado), curl femoral sentado (estirado, Maeo 2021) vs acostado (acortado), extensión de tríceps sobre la cabeza (estirado, Maeo 2023) vs pushdown (acortado), curl inclinado (estirado), elevación lateral con mancuerna (⬆, acortado), aperturas con mancuerna (⬇, estirado), pantorrilla de pie (estirado, Kassiano 2023). Filtradas por tipo (la elevación lateral en polea NO hereda la de mancuerna). Se ven con `~` y borde punteado, y **solo se vuelven tuyas si tocas esa fila** al guardar; "no sé" es una respuesta que apaga la sugerencia.
  - **`openExProfile(name)`**: músculos del motor (primario + hasta 3 secundarios con peso, sobre `targetsFor`; solo se guarda `exCat.targets` si los tocas), resistencia (con selector de gym si es máquina y existe en varios) y longitud, con la razón de la sugerencia. **Acceso:** catálogo (el ▢ elige para unir; **tocar el nombre abre el perfil**; cada fila muestra el perfil corto, `profileTag`: `⬇~ estirado`), pantalla de músculos ("perfil ›") y el **editor de máquina** de la sesión (resistencia de ESTA máquina en ESTE gym, junto a marca y setup).
  - `mergeExercises` lleva el perfil de las fuentes (resistencia por gym, libre y longitud; lo del destino gana).
  - Self-check nuevo **`_exProfileSelfCheck`** (17 en total). Verificado en preview: sugerencias en sldl/curl sentado/elevaciones/pushdown, confirmar tocando (longitud → tuya, resistencia sin tocar → sigue sugerida), máquina guardada solo para AGON GYM, vuelta al catálogo.
- **✅ DONE (v228, 2026-09-17) — músculos canónicos detrás de las etiquetas del dueño (PT2 de `fix.md`, fase 1 de 4).** Plan completo: `X:\claude-config\plans\greedy-imagining-minsky.md` (v228 canónico → v229 perfil del ejercicio → v230 recuperación por músculo + //MÚSCULOS → v231 diagnóstico estímulo vs recuperación).
  - **Hallazgo:** la capa canónica existía (`MUSCLE_ALIAS`/`canonM`/`VOL_LANDMARKS`) pero su único consumidor, `volStatus`, no lo llamaba nadie: todo agregaba por la etiqueta cruda. Sus 13 etiquetas reales: `lats delts chest quads hams calfs tricep mback bicep abs adductor glutes neck`; **`delts` mezcla elevaciones laterales y press**, así que el músculo real se resuelve POR EJERCICIO.
  - **`MUSCLES`** (18 ids estilo RP: `chest lats upper_back traps front_delts side_delts rear_delts biceps triceps forearms quads hamstrings glutes adductors calves abs neck lower_back`) con nombre corto en su estilo, nombre en español, grupo, **landmarks [MEV,MAV,MRV]** (los de `VOL_LANDMARKS` + deltoides separados; `null` = sin guía, nunca inventado) y **ventana de recuperación inicial estimada** (se usará en v230). **`MUSCLE_GROUPS`** para etiquetas ambiguas (`delts`, `back`, `legs`, `arms`). **`LABEL_CANON`** (sin acentos vía `mNorm`): `calfs→calves`, `tricep→triceps`, `mback→upper_back`, `hams→hamstrings`, `adductor→adductors`, `abductor→glutes`… `muscleCanon(label)` = override del usuario (`db.muscleMap`, aditivo) › tabla › desconocido.
  - **`targetsFor(ex)` / `exTargets(sw,ei)`**: músculos del MOTOR de un ejercicio `[{id,w,label,src}]` = override del ejercicio (`db.exCat[name].targets`, src *user*) › cada etiqueta mapeada (*auto/user*) › grupo resuelto por el nombre (`resolveGroup`: lateral→side, press→front, rear/face pull→rear, pulldown→lats, row→upper_back…, src *inferred*) › sin resolver (`id:null`, *unknown*: nunca adivina). `muscleLabel(id)` muestra SU etiqueta cuando exactamente una mapea directo, si no el nombre corto (o `db.muscleNames[id]`). Todo en lectura: aplica a todo el historial sin reescribir nada; **ninguna etiqueta se renombra** (regla v148 intacta).
  - **//EFFECTIVE VOLUME**: cada fila (su etiqueta) escala contra MEV y MRV de su músculo real (marca MRV en `--warn`) y dice "bajo MEV 8 / cerca de MRV / sobre MRV"; sin guía (`adductor`) conserva la marca ~10. `volStatus` lee `landmarksForLabel`.
  - **Pantalla "músculos"** (menú `u/` → "músculos · cómo los cuenta el motor", `openMuscleMap`): cada etiqueta con su músculo real (auto o elegido) y los ejercicios de etiquetas de grupo con su resolución ("~ por el nombre" o "fijado"), editables.
  - **Arreglos:** `mergeExercises.norm` (bug de v224: si cambiaba el primario, el `muscles[]` viejo sobrevivía y `exMuscles` devolvía el primario ANTERIOR); el merge lleva el `targets` fijado de una fuente; `setsWeek` pasa a `musclesOf` (leía músculos congelados mientras `tensWeek` leía los vivos); `muscleSuggest` incluye los músculos que solo usa como secundarios.
  - Self-check nuevo **`_muscleSelfCheck`** (16 en total). Verificado en preview con su vocabulario real: `calfs/lats/mback/quads/abs/neck bajo MEV`, `adductor` sin guía; press de hombro fijado como deltoides frontal conservando tríceps 0.25; override de etiqueta persistido y revertido; etiquetas intactas.
- **✅ DONE (v227, 2026-09-17) — gráficas nuevas con la especificación de las capturas de Bevel (fase 5 de `fix.md`).**
  - **Raíz:** `lineChart` hacía `(vals).map(v=>+v||0)`: un día sin registro era un punto real en 0 que además aplastaba la escala contra el techo ("un día que no se loggeó se pone hasta abajo"; su caso de SpO₂ en Bevel: "sin data no hay data"). Ahora `chartNums` convierte `null`/''/NaN — y el 0 con `opt.gap0` en las métricas diarias donde 0 = no registraste (pasos, agua, sueño, kcal, ánimo, FC reposo/HRV/energía) — en **hueco**: el trazo se parte en tramos, sin relleno ni punto cruzándolo; un día aislado conserva su punto; la escala sale solo de valores reales. El 0 real sigue siendo dato donde existe (auto-percepción 0/10, semanas de volumen 0). De paso: cada gradiente con id propio (antes ~15 `id="lcg"` repetidos) y sin deformación por `preserveAspectRatio="none"` (trazos `non-scaling-stroke`, puntos = trazos de largo cero con remate redondo). Tiles igual de mínimas.
  - **Detalle de métrica** (`openMetricDetail`, ahora junto al renderer): cabecera con el último valor, fecha y promedio, y a la derecha **estado contra TU rango normal** ("en rango / sobre lo normal / bajo lo normal · tu normal 10,382 – 17,271"), calculado con `normalBands` = percentiles 15–85 de los 90 días previos a cada día (mín. 7 con dato) — nunca tablas externas. Acentos solo en el estado, según `metricBetter` (FC reposo: bajar es bueno; pasos/sueño/HRV: subir; peso según el objetivo). Gráfica de 150 px: banda del rango normal en `--o10`, línea y puntos en `--fill`, último punto con halo, **promedio punteado con su pastilla**, 3 etiquetas Y a la derecha y 4 fechas abajo (HTML encima del SVG para que el texto no se deforme), eje de tiempo real. Periodos **7D · 15D · 30D · 3M · 6M · 1A** + **▦ rango custom** (dos fechas) + **‹ ›** para paginar la misma ventana hacia atrás (`mdPage`; `metricSeries(key,days,end)` y `scSeries` aceptan fecha final vía `daysEnding`). **Tendencias** (`trendRows`): 3 · 7 · 14 · 30 · 90 días = promedio de los últimos N vs los N anteriores, con "antes → ahora", flecha acentuada solo si el cambio supera ~1 error estándar (1.41·σ/√N) y mini-sparkline de la media móvil de 7 días.
  - **Gestos** (motor de pointer events delegado, `touch-action:none` solo sobre la gráfica): **mantener y deslizar** = línea vertical punteada + punto + etiqueta flotante con valor y día (reemplaza al viejo `chartpt`, que solo hacía un toast); **pellizco ↔** = cambia el periodo por la escalera de esa gráfica (abrir = más corto, cerrar = más largo); **pellizco ↕** = zoom vertical alrededor de tu promedio (más agresivo o más plano, 0.5×–6×, indicado junto a las fechas); **doble toque** = zoom normal. Los detalles de **volumen** (escalera 8/12/26/52 semanas) y **e1RM** (30/90/180/365 días) usan la misma gráfica completa y los mismos gestos.
  - Self-check nuevo **`_chartSelfCheck`** (15 en total): un hueco parte la línea, el día aislado conserva su punto, 0 = sin registro no dibuja punto ni aplasta la escala (el caso 98/97/—/—/96 usa todo el alto), el 0 real sí se dibuja, gradientes únicos, banda plana y sin historia suficiente, tendencia significativa vs ruido, scrub solo sobre días con dato. Verificado en preview con 200 días de pasos con huecos: la línea se corta, banda y promedio, "en rango", scrub "19,802 · 06 sep", pellizcos 30D→15D y 15D→30D→3M, zoom vertical en ambos sentidos y doble toque; tiles con tramos partidos e ids únicos; detalles de volumen y e1RM sin errores.
- **✅ DONE (v226, 2026-09-17) — suplementos dentro de macros, agenda fuera de la barra y racha comida-o-gym con calendario (fase 4 de `fix.md`).**
  - **Suplementos con menos fricción** (el dueño pidió "mejor propuesta de UI, menos fricción, integración correcta", no quitarlos). Causa del "se colapsan o se despliegan cuando haces un check": `renderStack` abría **solo el primer bloque con algo pendiente**, así que al marcar el último de AM el re-render cerraba AM y abría PM. El check diario se muda a **macros** (`suppStripHTML(curDate())`, entre la tarjeta de kcal y //LOG): un chip por toma debida ese día, agrupados por momento (AM · PM · ENTRENO · OTROS; un item con varios momentos sale en el primero, porque hay 1 tick por día), **un toque = tomado** con su hora real (`suppQuickTick`; otro toque lo deshace), lo tomado se va al final en gris con ✓ en acento, cabecera `//SUPPS · 2/4 [administrar]`. Sirve también para días pasados (usa la fecha que estés viendo). Sus nutrientes siguen sumando en `macroTotals`. El stack queda como pantalla de **administración** (menú `u/` → "suplementos · administrar", o `[administrar]`): todos los bloques abiertos siempre y `stackToggleTick` con `reRender()`.
  - **Agenda fuera:** la barra queda en **progress · gym · macros** (`renderNav`). No se borra ningún dato ni código: `db.stack`, `db.adhoc` y `renderAgenda` siguen ahí (y en backup/export).
  - **Racha nueva** (`activityDays`, `streak`, `bestStreak`): cuenta días con **registro manual de comida O de gym** (bits 1 = comida, 2 = gym; un descanso registrado con "rest" cuenta como gym); pasos/peso/sueño no cuentan porque llegan solos. Se corta solo el día sin ninguno de los dos; hoy se perdona hasta medianoche. En progreso, la tira de 7 puntos (solo gym) se reemplaza por **`streakCardHTML`**: racha en grande, mejor racha, "este mes N/M días registrados · %", días completos (ambos), estado de hoy y el **calendario de dos meses** (`streakCalendarHTML`, anterior | actual, `D L M M J V S`): una pastilla por día — vacía `--track`, comida o gym `--o60`, comida + gym `--good` (el único acento), hoy con contorno y punto si aún no hay registro, futuro sin relleno; leyenda abajo. **Toque en un día** (`openCalDay`): solo comida → macros en esa fecha; solo gym → hoja de la sesión; ambos → pregunta cuál. "streak: N" de la barra de estado lleva a esa tarjeta.
  - Self-check nuevo **`_streakSelfCheck`** (14 en total; fechas relativas a hoy, sin `save()`): solo comida, solo gym con hoy perdonado, un día sin nada corta, descanso registrado cuenta, mejor racha histórica, calendario marcando comida + gym. Verificado en preview: chips por momento, toque marca/deshace con hora, 2/4; racha 7 · mejor 10 · 14/17 = 82 % · completos 3; toque en un día de solo comida abre macros en esa fecha y uno con ambos pregunta; los bloques del stack ya no se cierran al marcar.
- **✅ DONE (v225, 2026-09-17) — historial: gym editable, compartir compacto, ✓ a la derecha y deslizar para borrar (fase 3 de `fix.md` + ajuste pedido en vivo).**
  - **Gym de una sesión pasada.** `pickgym` solo escribía `db.activeGym`/`activeWork`: una sesión guardada en el gym equivocado había que borrarla y re-teclearla con sus horas. `openSessionEdit` gana un selector **GYM** (`db.gyms` + el que ya tuviera) y `ss_save` lo escribe; como pasa por `save()`→`bumpIdx()`, los ▲▼ se recalculan contra el historial de ESE gym. La marca de máquina grabada en la sesión no cambia (es lo que registró ese día). El botón pasa de "guardar fecha/hora" a "guardar".
  - **Compartir compacto** (`renderShareSession`, `shareExLines`, `_shWR`, `_shRir`). Antes: tres números de 24 px, chips "60 × 11" sin unidad, sin RIR, sin drops y con los `_pf`. Ahora, con la densidad de //EFFECTIVE VOLUME: cabecera en una línea (`9 sets · 3 ejercicios · 1h04 · T 12.0`), un bloque por ejercicio (`[bi] nombre` + `músculo · unidad · T`) y **todas sus series en una línea**: `60×11 RIR2 · ↓35×8 RIR0 58% · 60×9 F`. La unidad va una vez por ejercicio si todas coinciden y por serie si mezcla (`80kg×8 · 12pla×6`); unilateral `R/L 70×10` cuando ambos lados son iguales, `R … · L …` cuando no; filtra `_pf`. Pie con gym y volumen en kg (`kgLoad`, placas fuera). CSS `.shhead/.sxr/.sxh/.sxs/.shfootline`; `.shfoot` 26→12 px.
  - **✓ en la última columna, borrar deslizando** (pedido del dueño en vivo: "el de confirmar set en donde está la X de borrar y para borrar que sea deslizando hacia la derecha"). `setnHTML` vuelve a ser solo el número (sigue llevando el color de zona) y ya **no** confirma; el control nuevo `doneCellHTML` (`.dchk`, 30×36 — la última columna de todas las rejillas pasó de 22 a 30 px) vive donde estaba la ✕; en unilateral es `.pairdone` a la derecha del par (28 px, alto del par). La ✕ desaparece de las filas: **deslizar la fila a la derecha ≥76 px la borra** (vivo e historial, `data-swipe` en cada `.srow`/`.pair`). El gesto solo se engancha si es claramente horizontal (si no, manda el scroll de `#view`), nunca arranca sobre un input/select/✓/grip, se ve la fila desplazarse y desvanecerse, un deslizamiento corto vuelve a su sitio, y `_swipeGuard` evita que el `click` posterior dispare algo. La lógica de borrado salió del dispatcher a `delSetLive`/`delSetHist` (una serie de trabajo se lleva sus drops; solo pregunta si esos drops tienen datos). Pista "desliza una serie → para borrarla" una sola vez, bajo el primer ejercicio.
  - Verificado: `node --check`, `?selftest=1` 13/13, y en preview: ✓ ○→✓ en la última columna, el número ya no confirma, deslizamiento largo borra (3→2), corto y vertical no; gym de una sesión guardada CAMPESTRE→AGON persistido; compartir con drop, F, mezcla kg/placas y unilateral.
- **✅ DONE (v224, 2026-09-17) — identidad de ejercicios: unir duplicados, disponibilidad por gym, `[uni]/[bi]` en todo y encabezado nuevo (fase 2 de `fix.md`).**
  - **Unir duplicados (`mergeExercises`).** El dueño tenía el mismo ejercicio dos veces ("sldl" y "bb sldl") y varios marcados como máquina que hace con peso libre; `knownExercises()` agrupa por nombre en minúsculas, así que eran dos historias distintas. El **catálogo deja de ser solo lectura**: cada fila se selecciona (`▢/▣`) y con dos o más aparece `[unir]` → modal con **nombre final** (precargado con la fuente de más sesiones), músculo, tipo, uni/bi, unidad y **en qué gyms existe**. El motor reescribe `db.split` (normaliza la configuración pero **cada instancia conserva su `id`**, que es su identidad), todas las `db.sessions` y la sesión viva, **re-vinculando `exId` con la regla de `fixDuplicateExIds`** (match único por día+nombre → ese id; ambiguo o inexistente → `null`, que la escalera de `baselineSetsFor` resuelve por nombre+variante sin adivinar). Las **máquinas se re-clavan gym por gym y solo las claves por NOMBRE** (las `#id` siguen válidas porque el id no cambia): si dos fuentes tienen registro en el mismo gym, el modal pregunta cuál gana. Lo ya grabado en cada sesión (marca, setup, pesos, fechas) **no se toca**. Respaldo previo en **su propia clave** `gymtrk_db_v1_premerge` — nunca `LS_BACKUP`, que `save()` pisa cada pocos segundos.
  - **Disponibilidad por gym (`db.exCat`, aditivo).** `{name,gyms[],type,unilateral,unit,muscle,muscles,mergedFrom[]}` por nombre, escrito por el merge; `exGymsFor()` lo une con los gyms donde realmente se entrenó. Lo consume el catálogo (etiquetas) y el picker "elige ejercicio", que ahora **pone primero los que existen en el gym donde entrenas hoy** y marca los demás como "· otro gym". **Nunca filtra**: entrenar en otro lado no puede hacer desaparecer un ejercicio de la lista.
  - **`[uni]`/`[bi]` en TODOS los módulos** (nuevo `latTxt()`, versión en texto plano de `exLatTagHead`): preview //NEXT (antes un "uni" suelto al final), editor de split (antes "· R/L"), catálogo, hoja de sesión, compartir y las listas FUERZA///RECORDS (sus agregados ahora guardan la lateralidad). La lateralidad es identidad — uni y bi nunca son el mismo ejercicio.
  - **Encabezado del ejercicio en la sesión, con su mockup:** `[bi] pushdown` en la primera línea y `[cable] [+ nota]` en la segunda (`.exsub`). **Se elimina el placeholder `+ machine`** ("sale putas machine cuando ya se puso que es cable"): el **tipo ocupa ese sitio** y es el que abre el editor de marca/setup (`variantChips(ex,{act,exi})`); la marca solo se muestra si existe y el `[+ nota]` desaparece en cuanto hay nota. Mismo formato de dos líneas en el editor de historial, de solo lectura.
  - **El split que sí entrenas (`dayDrift`/`adoptDaySource`).** Causa: desde v213 la sesión se siembra de `daySource()` (última sesión real del día) y **ningún cambio hecho en vivo vuelve al split**, que seguía siendo el de `myStarterSplit()` — de ahí "no me sale mi split actualizado". El editor ahora **detecta la diferencia por día** y ofrece `[adoptar]`: confirma mostrando fecha y lista, conserva el `id` de cada instancia que ya existía (su historial y su máquina cuelgan de ahí), acuña id nuevo para las que no, y toma el conteo de **series de trabajo** (los drops no son series del plan). Nunca automático.
  - Self-check nuevo **`_mergeSelfCheck`** (13 en total): corre el motor real sobre una db de juguete que se restaura en `finally` **y protege el respaldo pre-merge de verdad** (abrir `?selftest=1` no puede pisarle su copia). Verifica nombres unidos, split normalizado sin perder el id, tipo corregido, re-vinculación única, ambiguo → `null`, pesos/fechas intactos, la máquina elegida ganando en su gym, el otro gym conservando la suya, sin claves del nombre viejo, y `knownExercises` viéndolos como uno. Verificado además en preview con su caso real: `sldl` + `bb sldl` (una en AGON, otra en CAMPESTRE, una mal marcada como máquina) → un solo `SLDL` de peso libre, ambas sesiones re-vinculadas, catálogo con los dos gyms; y el editor de split detectando la deriva y adoptándola.
- **✅ DONE (v223, 2026-09-17) — el logueo en vivo (fase 1 de `fix.md`).** Reporte del dueño: "está medio ortopédico el tema de loggear... siempre se me scrollea hasta arriba... luego se autopone de que ya se completó el set cuando apenas lo voy a escribir y luego me pide la ventana emergente para confirmar que se ha terminado". Cinco defectos distintos, todos verificados en el código, no uno solo:
  - **El scroll saltaba al inicio** porque `render()` hace `v.scrollTop=state._scroll||0` y solo `addset`/`dropset`/`delset` guardaban la posición. Nuevo **`reRender()`** (snapshot de `#view.scrollTop` + `render()`) y lo usan ya TODAS las interacciones de sesión e historial (`done`, `fs`, `h_done`, `h_fs`, `h_addset`, `h_dropset`, `h_delset`, el `change` de `h_setf`, `idle_continue`, `pickgym`, `togglenoadv`).
  - **Tocar el RIR perdía el valor:** el listener de `click` sobre un `<select>` re-renderizaba con el picker nativo ABIERTO → el `<select>` original quedaba huérfano y su `change` ya no burbujeaba a `#app`. Ahora ese toque confirma **en sitio** (sin re-render): limpia el gris de ESE campo, refresca badge/zona/% y guarda.
  - **"Se marcaba completada sola":** un toque en cualquier campo limpiaba `_pf` de la serie ENTERA → `setComplete` pasaba a true y `renderSetRow` la emitía `data-locked readonly` (de ahí el `confirm('¿Editar set completado?')` al siguiente toque). Ahora lo pendiente se lleva **por campo** (`_pfF={w,r,rir}`, `_pfN` = cuántos nacieron; `pfHas/pfClearField/pfClearAll/pfAdd/pfMark/pfTouched`), `_pf` se conserva como el agregado "queda algo pendiente" (así `setPersistable`/`setComplete`/tensión/`saveSession` siguen leyendo UNA bandera) y **el candado vive solo en el editor de historial** — en sesión viva nada se bloquea. En unilateral el otro lado ya no se auto-confirma: hereda el peso como sugerencia gris (se parchea también su clase `.pf` en el DOM). Series viejas sin `_pfF` conservan la semántica de todo-o-nada.
  - **Sin pérdida de datos:** como confirmar un campo ya no confirma la serie, una serie **escrita a mano pero sin ✓** se habría tirado al guardar (`setPersistable` descarta `_pf`). `touchedSets(w)` la detecta (algún campo ya tocado + peso y reps) y `saveSession`/`finalizeIdleSession` la confirman explícitamente, diciéndolo en el diálogo ("1 serie escrita sin ✓ se cuenta como hecha"). Las sugerencias que nunca tocaste se siguen descartando.
  - **"Me pide confirmar que terminé mientras logueo":** `sessionIdleEnd` solo miraba `doneAt`/`resumedAt`/`startMs` — teclear no era actividad, así que a los 10 min saltaba el modal no-descartable. Ahora `setField` y cualquier toque dentro de la sesión sellan `w.lastTouch` y `sessionIdleEnd` lo cuenta. Tras inactividad real el modal sigue saliendo igual.
  - **✓ por serie** (decisión suya, "un ✓ por serie"): la celda del número ya era el toggle de `done`; ahora **muestra el estado** (`○` pendiente / `✓` hecha, apilado bajo el número para que quepa un "1.5", vía `setnHTML()` — mismo control en vivo y en historial) y ese toque confirma la serie completa, la sella con su hora y arranca el descanso.
  - **Dónde vas:** barra persistente **AHORA · ejercicio · serie N/M** (`#nowbar`, fuera de `#view` como el descanso, se pinta con `updateNowBar()` en cada render; toque = scroll a esa serie), guía `.ex.current` en el ejercicio en curso (`currentSetTarget()`) y, tras cada ✓, el foco va a la **siguiente serie de ese ejercicio** (`nextSetTarget()` + `state._focusSet`) en vez de a la primera serie sin RIR del documento. De paso se define `--o70` (lo usaba `.bwchip` sin existir).
  - Self-check nuevo **`_loggingSelfCheck`** (prefill por campo, confirmar el RIR no confirma peso/reps, legacy todo-o-nada, ✓ que limpia todo, candado solo en historial, rescate de la serie escrita sin ✓, AHORA/foco, `lastTouch`). Verificado: `node --check`, `?selftest=1` **12/12**, y en preview con sesión real — tocar el RIR sin que salte el scroll ni se pierda el valor (scrollTop idéntico, `<select>` vivo, solo `rir` confirmado), ✓ que confirma y arranca descanso, barra AHORA, espejo unilateral gris, guardado que conserva la serie escrita sin ✓ y no filtra `_pf*` a la sesión guardada, y 10 min tecleando sin modal de cierre.
- **✅ DONE (v222, 2026-09-17) — cascarón nativo iOS/Android + puente con Salud / Health Connect.** Decisión del dueño: "planear la ruta para sacar la app para iOS y Android". **`index.html`:** `NATIVE_APP` (Capacitor inyectado) / `capP()` / `withTO()`; en la app el db se espeja a `Library/db_A.json`/`db_B.json` alternados (`nativeMirrorSoon`, debounce 1.5 s — el WebView puede desalojar localStorage) y `nativeBoot()` adopta el más nuevo si localStorage quedó vacío/con menos datos; `doExport`/`exportMarkdown` escriben en Documents y abren la hoja de compartir (`nativeShareFile`); el service worker solo se registra en la web (en la app se desregistra); el botón del Atajo llama al sync nativo. **`healthSyncNative(reason)`** (abrir / `App.resume` / botones de //SALUD "conectar Salud", "sincronizar ahora", "Health Connect"; lock + 5 min salvo manual): `requestAuthorization` solo lectura (+historial en Android); totales diarios por **agregado** (pasos, energía activa, FC reposo, FC mín/media/máx — el día lo decide el punto medio del bucket), basal y HRV por muestras (mediana diaria, SDNN iOS / RMSSD Android); sueño: muestras por fase agrupadas en bloques (hueco >60 min), sin "inBed" si hay fases, **una fuente por noche** (la de más minutos dormidos, evita contar doble iPhone+pulsera); entrenos 14 d (paginados); FC SOLO en ventanas de sesiones ≤72 h no finales, en trozos de 30 min con `limit` explícito (el plugin trae 100 por defecto), avisa si llegan muestras agrupadas (series) → todo entra por `ingestHealth`. Permisos: Android revisa `readDenied`; iOS no puede distinguir "negado" de "sin datos" → aviso heurístico en el log. Errores (teléfono bloqueado, timeout) se registran y reintentan; nunca cuentan como cero. **`native/`:** `package.json`/lock, `capacitor.config.json` (`io.github.dxxnielbot.gymtrk`), `scripts/{guard-drive,copy-web}.cjs` (www = index+manifest sin sw.js, o la probe), `patch-ios.sh` (plutil: NSHealthShare/Update, cámara, fotos, UIFileSharing, versión), `patch-android.sh` (minSdk 26, CAMERA, READ_HEALTH_DATA_HISTORY, quita WRITE_* del plugin, versionCode; idempotente), `App.entitlements`, `resources/privacypolicy.html`, `probe/index.html` (spike: permiso, FC 6 h / último entreno con veredicto de resolución, sueño, pasos/FC reposo) y `README.md`. **Sin builds disparados** (acción del dueño). Verificado: `node --check`; web `?selftest=1` 11/11 y //SALUD en modo web; arnés temporal (no versionado) con Capacitor simulado → modo nativo, espejo a archivo, "conectar Salud" (90 días, 90 noches con horas dormidas, entreno ligado a la sesión, FC por serie en 5 series), exportar por compartir, y **recuperación tras borrar localStorage** (184 registros adoptados desde el archivo); YAML de workflows parseado; `bash -n`; `patch-android.sh` probado dos veces sobre un proyecto simulado (idempotente, XML válido); `guard-drive` bloquea en I:.
- **Pending owner action (native):** chequeo de resolución de FC en la app Salud; correr *native-ios* con `target=probe`, instalar con Sideloadly (o Impactor), probar permiso/FC/sueño; luego `target=app` + migrar respaldo; para Android crear keystore + 4 secrets. Ver `native/README.md`.
- **Pending owner action:** deploy the Gemini Worker (`proxy/README.md`) and set `AI_PROXY_URL` to enable AI label reading.
- Barcode scanner is now **photo-capture + `Quagga.decodeSingle`** (laptop v8, replaced flaky live scan) — confirm reliability on device.
- Refine OCR accuracy after device testing.
- (Stretch) real cross-device sync/account backend; icons for PWA. Possible: structured setup fields (seat/back/height) instead of free text; a "view/edit all machines for a gym" screen.

## Design language
**Fuente única: [`DESIGN_SYSTEM.md`](DESIGN_SYSTEM.md)** (constitución visual y de interacción, 2026-09-18; reemplaza a `STYLEMAP.md`). Toda UI nueva se construye con ese documento y se audita con `node tools/ds-audit.cjs` (sin regresiones en P0/P1). Resumen: fondo `#000` (no #1e1e1e), JetBrains Mono única (300/400/500/700/800; **600 no está cargado**), jerarquía por opacidad de blanco, `//` para secciones, `[ ]` para acciones de texto, `[uni]/[bi]` al frente = solo lateralidad, color solo semántico (`--info` azul = solo déficit), **anillos solo en macros**, glass solo en chrome flotante (nav/sheets/toast), tabla de sesión densa y afilada, lenguaje de honestidad (`~` estimado, punteado = sugerido, gris = prefill, hueco = sin dato, sin score único). Marco 393×852, gutter 18 px.
