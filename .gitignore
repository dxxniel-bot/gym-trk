# native shell (Capacitor): dependencies and generated platform projects never go in git (and never on the I: drive)
native/node_modules/
native/www/
native/ios/
native/android/
native/plugins/*/node_modules/
*.ipa
*.apk
*.aab
*.keystore
*.jks
.DS_Store

# copia de la versión anterior para tools/ds-diff.html
_pre.html
_seed.json
